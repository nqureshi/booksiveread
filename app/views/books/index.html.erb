<p class="lead">
  The King's Period has mandated that a book database be created. This shall contain the Books of all Kings. Please see below for the selection. 
</p>

<table class="table">
  <th>Number</th>
  <th>Title</th>
  <th>Author</th>
  <th>Created</th>
  
  <% @books.each do |book| %>
    <tr>
      <td><%= book.id %></td>
      <td><%= link_to book.title, book_path(book.id) %></td>
      <td><%= book.author %></td>
      <td><%= book.created_at.strftime("%d %B, %Y")%>
    </tr>
  <% end %>
</table>

<%= link_to "Add New Book", new_book_path, :class => "btn btn-primary" %>
